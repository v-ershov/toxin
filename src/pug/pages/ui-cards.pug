extends /pug/templates/_base.pug

block vars
  - var keywords = 'important, keywords';
  - var description = 'short description';
  - var title = 'UI Cards';

block body
  +wrapper(['ui', 'gray'])
    h1 UI Cards
