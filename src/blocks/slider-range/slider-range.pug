//- Options:
//- 🞂 labelPrimary: string - лейбл поля слайдера (первичный)
//- 🞂 min: number          - мин. значение слайдера
//- 🞂 max: number          - макс. значение слайдера
//- 🞂 start: number        - начальное значение левого ползунка слайдера
//- 🞂 end: number          - начальное значение правого ползунка слайдера
//- 🞂 step: number         - шаг ползунков слайдера
//- 🞂 unit: string         - единица измерения значений слайдера
//- 🞂 text: string         - текст слайдера

include /blocks/label/label.pug

mixin slider-range(options)
  -
    const defaults = {
      labelPrimary: 'undefined',
      min: 0,
      max: 0,
      start: 0,
      end: 0,
      step: 0,
      unit: '',
      text: '',
    };

  -
    const {
      labelPrimary,
      min,
      max,
      start,
      end,
      step,
      unit,
      text,
    } = Object.assign(defaults, options);

  - const labelSecondary = `${start.toLocaleString('ru')}${unit} - ${end.toLocaleString('ru')}${unit}`;

  .slider-range
    +label({
      primary: labelPrimary,
      secondary: labelSecondary,
    })
      input._input(type='hidden', value=labelSecondary)
    ._slider(data-min=min, data-max=max, data-start=start, data-end=end, data-step=step, data-unit=unit)
    if text
      ._text
        | #{text}
