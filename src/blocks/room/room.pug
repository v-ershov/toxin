//- Parameters:
//- string[[]] list    - список изображений комнаты, где каждый пункт (массив) содержит:
//-                      - имя файла с изображением комнаты
//-                      - значение атрибута «alt» у изображения
//- string     number  - номер комнаты
//- string     tag     - тег комнаты
//- string     price   - цена комнаты за сутки
//- int        rating  - значение рейтинга (от 0 до 5)
//- string     reviews - количество отзывов

//- Examples:
//- +room([
//-   ['888-1-small.jpg', 'Room 888 (1)'],
//-   ['888-2-small.jpg', 'Room 888 (2)'],
//-   ['888-3-small.jpg', 'Room 888 (3)'],
//-   ['888-4-small.jpg', 'Room 888 (4)'],
//- ], '888', 'Люкс', '9 990₽', 5, '145')

include /blocks/rating/rating.pug
include /blocks/room-info/room-info.pug
include /blocks/room-price/room-price.pug

mixin room(list, number, tag, price, rating, reviews)
  .room
    ._slider
      each item in list
        img._slide(src=require(`~/content/rooms/${item[0]}`).default, alt=item[1])
    ._info
      ._row.-baseline
        ._cell
          +room-info(number, tag, ['small'])
        ._cell
          +room-price(price)
      ._row
        ._cell
          +rating({
            value: rating,
          })
        ._cell
          span._reviews
            | #{reviews}
          | &nbsp;Отзывов
      a._link(href='/', target='_blank')
        | Детали
