//- Options:
//- 🞂 tag: string  - имя тега
//- 🞂 text: string - текст, к которому будут применены стили указанного тега

mixin ui-type(options)
  -
    const defaults = {
      tag: 'undefined',
      text: 'undefined',
    };

  -
    const {
      tag,
      text,
    } = Object.assign(defaults, options);

  .ui-type(class=`ui-type--theme--${tag.toLowerCase()}`)
    ._tag
      | #{tag}
    ._text
      | #{text}
