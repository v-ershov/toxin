//- Options:
//- 🞂 text: string   - текст ссылки
//- 🞂 mods: string[] - модификаторы блока
//- 🞂 attrs: object  - атрибуты ссылки

mixin link(options)
  -
    const defaults = {
      text: 'undefined',
      mods: [],
      attrs: {},
    };

  -
    const {
      text,
      mods,
      attrs,
    } = Object.assign(defaults, options);

  a.link(class=getModifiers('link', mods))&attributes(attrs)
    | #{text}
