//- Parameters:
//- bool isSignUp=false - если true, то вместо формы «Sign In» отобразится форма «Sign Up»

//- Examples:
//- +main-sign
//- +main-sign(true)

include /blocks/form-sign-in/form-sign-in.pug
include /blocks/form-sign-up/form-sign-up.pug

mixin main-sign(isSignUp=false)
  main.main-sign
    ._background
    ._container
      ._form
        if isSignUp
          +form-sign-up
        else
          +form-sign-in
