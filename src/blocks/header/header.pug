//- Options:
//- 🞂 isLogin: boolean - если true, то вместо кнопок отображается ссылка на профиль пользователя

include /blocks/button/button.pug
include /blocks/header-submenu/header-submenu.pug
include /blocks/logo/logo.pug

mixin header(options)
  -
    const defaults = {
      isLogin: false,
    };

  -
    const {
      isLogin,
    } = Object.assign(defaults, options);

  header.header
    ._container
      ._logo
        +logo({
          modifiers: ['adaptive'],
        })
      button._hamburger(type='button')
        | Гамбургер-меню
        span._icon-layers
          span._icon-layer
          span._icon-layer
          span._icon-layer
      nav._nav(style='--height: 100vh;')
        ul._list
          li._item
            a._link.-bold(href='/change-me')
              | О нас
          li._item
            +header-submenu({
              title: 'Услуги',
              href: '/change-me',
              list: [
                ['Оформление виз', '/change-me'],
                ['Заказ экскурсий', '/change-me'],
                ['Конференц-залы', '/change-me'],
              ],
            })
          li._item
            a._link(href='/change-me')
              | Вакансии
          li._item
            a._link(href='/change-me')
              | Новости
          li._item
            +header-submenu({
              title: 'Соглашения',
              href: '/change-me',
              list: [
                ['Польз. соглашение', '/change-me'],
                ['Политика конф-ти', '/change-me'],
              ],
            })
      if isLogin
        ._user
          a._link(href='/change-me')
            | Юлий Цезарь
      else
        ._buttons
          ._button
            +button({
              text: 'Войти',
              modifiers: ['compact'],
              attrs: {
                href: '/change-me',
              },
            })
          ._button.-adaptive
            +button({
              text: 'Зарегистрироваться',
              modifiers: ['compact', 'fill'],
              attrs: {
                href: '/change-me',
              },
            })
