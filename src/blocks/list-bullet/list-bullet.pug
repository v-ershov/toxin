//- Options:
//- 🞂 list: [[      - список перечислений
//-   🞂 [0]: string - текст пункта списка
//-   🞂 [1]: string - значение атрибута «href» у пункта списка
//-                   (тогда пункт списка автоматически становится ссылкой)
//-   ]]

mixin list-bullet(options)
  -
    const defaults = {
      list: [],
    };

  -
    const {
      list,
    } = Object.assign(defaults, options);

  ul.list-bullet
    each item in list
      li._item
        if item[1]
          a._link(href=item[1])
            | #{item[0]}
        else
          | #{item[0]}
